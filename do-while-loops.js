// Do While Loops
// Evaluate cond AFTER statement is ran

let cond2 = 1;
do {
	console.log("Do do do " + cond2);
	cond2 += 1;
} while (cond2 <= 5);

console.log("End loop");

/* 
Something learned, can't have same variable
name in two different Javascript files
in the same directory.
*/