// Ternary operator
// Shortcut for if-else

let dollars = 1;

// (cond) ? (value if true) : (value if false)
console.log(dollars + (dollars > 1 ? " dollars" : " dollar"));

// if (dollars > 1) {
// 	console.log(dollars + " dollars");
// } else {
// 	console.log(dollars + " dollar");
// }
